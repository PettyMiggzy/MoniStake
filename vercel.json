{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd app/app && npm install && npm run build",
  "outputDirectory": "app/app/.next",
  "installCommand": "echo 'skipping root install — handled by buildCommand'",
  "framework": "nextjs",
  "git": {
    "deploymentEnabled": {
      "main": true
    }
  }
}
